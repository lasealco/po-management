# BF-17 — Bearer secret for POST /api/wms/tms-webhook (no app-session cookie; external TMS / middleware calls only).
# Generate a long random string for production. Leave unset locally unless testing the stub.
TMS_WEBHOOK_SECRET=
