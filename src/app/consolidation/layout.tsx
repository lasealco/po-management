import { PoMgmtWorkspaceLayout } from "@/components/po-mgmt-workspace-layout";

export default function ConsolidationLayout({ children }: { children: React.ReactNode }) {
  return <PoMgmtWorkspaceLayout>{children}</PoMgmtWorkspaceLayout>;
}
