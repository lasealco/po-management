/** Idempotent demo seed for `demo-company` (see `prisma/seed-invoice-audit-demo.mjs`). */
export const INVOICE_AUDIT_DEMO_SEED_CLI = "USE_DOTENV_LOCAL=1 npm run db:seed:invoice-audit-demo" as const;
